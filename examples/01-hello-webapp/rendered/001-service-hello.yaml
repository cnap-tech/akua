apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: hello
    app.kubernetes.io/managed-by: akua
    app.kubernetes.io/name: nginx
  name: hello
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app.kubernetes.io/instance: hello
    app.kubernetes.io/name: nginx
  type: ClusterIP
