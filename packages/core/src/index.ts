/**
 * @akua/core — low-level NAPI/WASM bindings to akua-core (Rust).
 *
 * Most consumers should use @akua/sdk instead. This package exposes the raw
 * FFI surface for power users and for the SDK's own internal use.
 *
 * Status: pre-alpha. No API yet.
 */

export const PLACEHOLDER = '@akua/core — bindings pending (milestone v4)';
