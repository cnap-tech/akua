/**
 * @akua/ui — Svelte components for Akua.
 *
 * Composable primitives for building package-authoring UIs:
 * - PackageEditor (file tree + Monaco)
 * - FormPreview (renders x-user-input fields)
 * - ManifestViewer (Kubernetes YAML with diff)
 * - TestRunner (live results)
 *
 * Used by hosted Package Studios; reusable in any Svelte app.
 *
 * Status: pre-alpha. Empty exports for now.
 */

export const PLACEHOLDER = '@akua/ui — components pending (milestone v5)';
